import { FieldValues } from 'react-hook-form';
import { NTimePickerField } from './models';
export declare const TimePickerField: <T extends FieldValues>({ className, control, defaultValue, fieldName, isHorizontal, isRequired, label, onChange, rules, shouldUnregister, tooltip, ...rest }: NTimePickerField.TProps<T>) => import("react/jsx-runtime").JSX.Element;
