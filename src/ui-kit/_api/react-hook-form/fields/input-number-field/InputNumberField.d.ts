import { FieldValues } from 'react-hook-form';
import { NInputNumberField } from './models';
export declare const InputNumberField: <T extends FieldValues>({ className, control, defaultValue, fieldName, isHorizontal, isRequired, label, onChange, rules, shouldUnregister, style, tooltip, ...rest }: NInputNumberField.TProps<T>) => import("react/jsx-runtime").JSX.Element;
