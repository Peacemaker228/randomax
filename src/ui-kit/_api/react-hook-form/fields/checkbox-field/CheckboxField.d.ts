import { FieldValues } from 'react-hook-form';
import { NCheckboxField } from './models';
export declare const CheckboxField: <T extends FieldValues>({ className, children, control, defaultValue, fieldName, isRequired, label, onChange, rules, shouldUnregister, style, tooltip, ...rest }: NCheckboxField.TProps<T>) => import("react/jsx-runtime").JSX.Element;
