import { FieldValues } from 'react-hook-form';
import { NTextAreaField } from './models';
export declare const TextAreaField: <T extends FieldValues>({ className, control, defaultValue, fieldName, isHorizontal, isRequired, label, onChange, rows, rules, shouldUnregister, style, tooltip, ...rest }: NTextAreaField.TProps<T>) => import("react/jsx-runtime").JSX.Element;
