import { ReactNode } from 'react';
import { Meta } from '@storybook/react';
import { NScrollArea } from '../models';
declare const meta: Meta<NScrollArea.TProps>;
export default meta;
export declare const DefaultScrollArea: (argTypes: NScrollArea.TProps) => ReactNode;
export declare const HorizontalScrollArea: (argTypes: NScrollArea.TProps) => ReactNode;
