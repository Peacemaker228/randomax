import { FileInput } from '../FileInput';
import { Avatar, Default, DefaultFiles, Directory, DragAndDrop, MaxCount, UploadManually, UploadOnly } from './components';
declare const meta: {
    title: string;
    component: typeof FileInput;
    parameters: {
        layout: string;
    };
    tags: string[];
};
export default meta;
export { Default, Avatar, DefaultFiles, DragAndDrop, UploadManually, Directory, UploadOnly, MaxCount };
