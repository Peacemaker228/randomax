import { TSize as TSizeC, TVariant as TVariantC } from '../../../models/ui';
import { NDateTime } from '../_common/models';
/**
 * Неймспейс с типизацией NTimePicker
 */
export declare namespace NTimePicker {
    type TPlacement = NDateTime.TPlacement;
    type TSize = TSizeC;
    type TStatus = NDateTime.TStatus;
    type TVariant = TVariantC;
    type TProps = NDateTime.TTimePickerProps;
}
